{
  "name": "metrif-scraper-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "railway:build": "npm ci --prefix scraper-api && npm run playwright:install --prefix scraper-api && npm run build --prefix scraper-api",
    "railway:start": "npm run start --prefix scraper-api"
  },
  "engines": {
    "node": ">=22"
  }
}
